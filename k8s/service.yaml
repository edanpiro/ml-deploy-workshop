apiVersion: v1
kind: Service
metadata:
  name: ml-api-svc
  labels:
    app: ml-api
spec:
  selector:
    app: ml-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer     # Cambiar a NodePort para minikube si no hay LB
